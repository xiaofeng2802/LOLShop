@using PagedList.Mvc
@using ThoConShop.Business.Dtos
@model ThoConShop.Web.Models.AccountIndexViewModel
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
@{ 

}

@Html.HiddenFor(a => Model.CurrentGankFilter)
@Html.HiddenFor(a => Model.CurrentPriceFilter)
@Html.HiddenFor(a => Model.CurrentSkinFilter)

<div class="row filter">
    <div class="col-md-12 filter-back-ground">
        <div class="btn-group">
            <button type="button" class="btn btn-default dropdown-toggle filter-button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @if (Model.CurrentGankFilter == null)
                {
                    <span>Tìm Theo Gank</span> <span class="caret"></span>
                }
                else
                {
                    <span>@Model.GanksFilter.SingleOrDefault(a => a.Id == Model.CurrentGankFilter).GankName</span> <span class="caret"></span>
                }
                
            </button>

            <ul class="dropdown-menu">
                @foreach (var gankFilter in Model.GanksFilter)
                {
                    <li>@Html.ActionLink(gankFilter.GankName, "Index", new { CurrentGankFilter = gankFilter.Id, CurrentPriceFilter = Model.CurrentPriceFilter, CurrentSkinFilter = Model.CurrentSkinFilter })</li>
                }
            </ul>
        </div>
        <div class="btn-group">
            <button type="button" class="btn btn-default dropdown-toggle filter-button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @if (string.IsNullOrEmpty(Model.CurrentPriceFilter))
                {
                    <span>Tìm Theo Giá</span> <span class="caret"></span>
                }
                else
                {
                    <span>@Model.CurrentPriceFilter</span> <span class="caret"></span>
                }
            </button>
            <ul class="dropdown-menu">
                @foreach (var price in Model.PriceFilter)
                {

                    <li>@Html.ActionLink(price, "Index", new { CurrentPriceFilter = price, CurrentGankFilter = Model.CurrentGankFilter, CurrentSkinFilter = Model.CurrentSkinFilter })</li>
                }
            </ul>
        </div>
        <div class="btn-group">
            <button type="button" class="btn btn-default dropdown-toggle filter-button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @if (Model.CurrentSkinFilter == null)
                {
                    <span> Tìm Theo Trang Phục</span> <span class="caret"></span>
                }
                else
                {
                    <span>@Model.SkinsFilter.SingleOrDefault(a => a.Id == Model.CurrentSkinFilter).SkinName</span> <span class="caret"></span>
                }
            </button>
            <ul class="dropdown-menu">
                @foreach (var skin in Model.SkinsFilter)
                {
                    <li>@Html.ActionLink(skin.SkinName, "Index", new { CurrentSkinFilter = skin.Id, CurrentGankFilter = Model.CurrentGankFilter, CurrentPriceFilter = Model.CurrentPriceFilter })</li>
                }
            </ul>
        </div>
        <div class="btn-group">
            <a class="btn btn-default filter-button" href="@Url.Action("Index")">
                Clear
            </a>
        </div>
    </div>
</div>

@Html.Partial("_bodyIndex", Model.DataSource)

<div class="row">
    <div class="col-md-12 body-pagging">
        @Html.PagedListPager(Model.DataSource, page => Url.Action("Index", new { page, CurrentGankFilter = Model.CurrentGankFilter, CurrentPriceFilter = Model.CurrentPriceFilter, CurrentSkinFilter = Model.CurrentSkinFilter }))
    </div>
</div>
